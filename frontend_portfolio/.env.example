# Frontend environment variables (React - Create React App)
# Copy this file to `.env` and adjust values as needed.

# Base URL for the Django contact-service (must include protocol and port).
# In local development, ensure CORS allows http://localhost:3000 from backend.
# Example Django service running at http://localhost:3011
REACT_APP_CONTACT_API_URL=http://localhost:3011

# Optional: Legacy EmailJS client-side sending (fallback if API URL not set)
REACT_APP_EMAILJS_PUBLIC_KEY=
REACT_APP_EMAILJS_SERVICE_ID=
REACT_APP_EMAILJS_TEMPLATE_ID=
